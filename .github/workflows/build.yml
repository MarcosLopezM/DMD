name: Compile LaTeX projects

on:
  push:
    paths:
      - "Tareas/T3/**"
      - "Tareas/T5/**"
  workflow_dispatch:

jobs:
  build-tarea-3:
    uses: MarcosLopezM/latex-workflow/.github/workflows/build-pdf.yml@main
    with:
      relative_path: Tareas/T3
      tex_name: main
      out_name: "Tarea3"
      # send_it: true
      email_to: "marcoslm@ciencias.unam.mx"
    secrets: inherit

  build-tarea-5:
    uses: MarcosLopezM/latex-workflow/.github/workflows/build-pdf.yml@main
    with:
      relative_path: Tareas/T5
      tex_name: main
      out_name: "Tarea5"
      # send_it: true
      email_to: "marcoslm@ciencias.unam.mx"
    secrets: inherit
